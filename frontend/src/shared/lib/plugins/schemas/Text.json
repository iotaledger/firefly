{
  "version": 1.0,
  "description": "Text component schema.",
  "properties": {
    "id": {
      "type": "string",
      "description": "Component id.",
      "enum": [
        "^\\d?\\.?\\d?\\.?\\d$"
      ]
    },
    "category": {
      "type": "string",
      "description": "Must be @iota/core.",
      "enum": [
        "@iota/core"
      ]
    },
    "children": {
      "type": "array",
      "description": "Component children."
    },
    "content": {
      "type": "string",
      "description": "Text to display."
    },
    "type": {
      "type": "string",
      "description": "Controls the type of text component.",
      "enum": [
        "h1",
        "h2",
        "h3",
        "h4",
        "p"
      ]
    },
    "secondary": {
      "type": "boolean",
      "description": "Secondary class on the component.",
      "default": false
    },
    "disabled": {
      "type": "boolean",
      "description": "Disabled class on the component.",
      "default": false
    },
    "classes": {
      "type": "string",
      "description": "Additonal CSS classes."
    }
  },
  "required": [
    "id",
    "category",
    "type"
  ],
  "propertiesSummary": [
    {
      "name": "id",
      "type": "string",
      "required": "Required",
      "text": "Component id. Must be unique for each component in a plugin."
    },
    {
      "name": "category",
      "type": "string",
      "required": "Required",
      "text": "Component category. Currently, only @iota/core category is supported."
    },
    {
      "name": "children",
      "type": "array",
      "required": "Optional",
      "text": "Component children. If both 'content' & 'children' are passed, 'children' takes precedence."
    },
    {
      "name": "content",
      "type": "string",
      "required": "Optional",
      "text": "Component content. If 'children' property is passed, this property will be ignored."
    },
    {
      "name": "secondary",
      "type": "boolean",
      "required": "Optional",
      "text": "When true, 'secondary' CSS class should be applied."
    },
    {
      "name": "disabled",
      "type": "boolean",
      "required": "Optional",
      "text": "When true, the component will be disabled."
    },
    {
      "name": "classes",
      "type": "string",
      "required": "Optional",
      "text": "Additional CSS classes used by the component."
    }
  ]
}