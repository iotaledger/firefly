// https://www.typescriptlang.org/tsconfig#strict
{
    "extends": "./tsconfig",
    "compilerOptions": {
        "strict": false
    },
    "files": [
        // add files here and change strict to true
        // save this step until after /shared has been converted to strict
    ]
}
