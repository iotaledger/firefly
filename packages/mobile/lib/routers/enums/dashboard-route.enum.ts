export enum DashboardRoute {
    AccountSwitcher = 'accountSwitcher',
    Activity = 'activity',
    AccountActions = 'accountActions',
    Filter = 'filter',
    Token = 'token',
    Init = 'init',
    Profile = 'profile',
    Receive = 'receive',
    Send = 'send',
}
