export enum DashboardRoute {
    AccountSwitcher = 'accountSwitcher',
    Activity = 'activity',
    Token = 'token',
    Init = 'init',
    Profile = 'profile',
    Receive = 'receive',
    Send = 'send',
}
