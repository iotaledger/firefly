export enum DashboardRoute {
    AccountSwitcher = 'accountSwitcher',
    Activity = 'activity',
    Init = 'init',
    ProfileActions = 'profileActions',
    Receive = 'receive',
    Send = 'send',
}
