/**
 * This is where the account manager Stronghold and other data is stored.
 */
export const BASE_FILE_PATH = `${__dirname}/../../temp`

/**
 * This is where the optional Stronghold backups are stored.
 */
export const BACKUP_FILE_PATH = `${__dirname}/../../temp/backups`

/**
 * The path of the environment variable files.
 */
export const ENV_FILE_PATH = `${__dirname}/../../.env`
