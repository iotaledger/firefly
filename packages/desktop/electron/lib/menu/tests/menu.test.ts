import { initMenu } from '../menu'

describe('File: menu.ts', () => {
    describe('Function: initMenu', () => {
        it('should be true', () => {
            expect(initMenu()).toEqual(undefined)
        })
    })
})
