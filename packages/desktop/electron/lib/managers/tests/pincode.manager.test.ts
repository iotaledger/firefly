import { PincodeManager } from '../pincode.manager'

describe('File: pincode.manager.ts', () => {
    describe('Function: pincode.manager', () => {
        it('should be true', () => {
            expect(true).toEqual(true)
        })
    })
})
