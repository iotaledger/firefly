import { NotificationManager } from '../notification.manager'

describe('File: notification.manager.ts', () => {
    describe('Function: notification.manager', () => {
        it('should be true', () => {
            expect(true).toEqual(true)
        })
    })
})
