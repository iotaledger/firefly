import { DeepLinkManager } from '../deep-link.manager'

describe('File: deep-link.manager.ts', () => {
    describe('Function: deep-link.manager', () => {
        it('should be true', () => {
            expect(true).toEqual(true)
        })
    })
})
