import { ISideDrawerState } from '../interfaces'

export const DEFAULT_SIDE_DRAWER_STATE: ISideDrawerState = {
    active: false,
    id: null,
    hideClose: false,
    preventClose: false,
    direction: null,
    overflow: false,
}
