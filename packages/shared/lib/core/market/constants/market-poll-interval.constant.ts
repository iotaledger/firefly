import { MILLISECONDS_PER_SECOND, SECONDS_PER_MINUTE } from '@lib/time'

const INTERVAL_IN_MINUTES = 1

export const MARKET_POLL_INTERVAL = INTERVAL_IN_MINUTES * SECONDS_PER_MINUTE * MILLISECONDS_PER_SECOND
