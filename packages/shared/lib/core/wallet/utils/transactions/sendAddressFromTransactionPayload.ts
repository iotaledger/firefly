import { ITransactionPayload } from '@iota/types'

// TODO: finalize this once we have the correct payload
export function sendAddressFromTransactionPayload(payload: ITransactionPayload): string {
    return ''
}
