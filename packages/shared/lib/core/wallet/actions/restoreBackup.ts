// If IGNORE_IF_COIN_TYPE_MISMATCH is provided, the SDK won't restore the client options.
// If IGNORE_IF_COIN_TYPE_MISMATCH == true, the SDK won't restore the client options, coin type
// and accounts IF the cointype doesn't match
// const IGNORE_IF_COIN_TYPE_MISMATCH = true

export async function restoreBackup(): Promise<void> {
    // const wallet = getSelectedWallet()
    // await wallet.restoreBackup(importFilePath, password, IGNORE_IF_COIN_TYPE_MISMATCH, bech32Hrp)
}
