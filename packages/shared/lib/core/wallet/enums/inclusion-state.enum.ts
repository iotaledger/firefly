// TODO: Remove this and use the SDK version.
export enum InclusionState {
    Pending = 'Pending',
    Accepted = 'Accepted',
    Confirmed = 'Confirmed',
    Finalized = 'Finalized',
    Failed = 'Failed'
}