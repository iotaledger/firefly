import { FormatIotaUnitOptions } from '../interfaces'

export const DEFAULT_FORMAT_IOTA_UNIT_OPTIONS: FormatIotaUnitOptions = {
    includeUnits: true,
    grouped: false,
    overrideDecimalPlaces: undefined,
}
