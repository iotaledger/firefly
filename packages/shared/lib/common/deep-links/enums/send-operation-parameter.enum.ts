/**
 * The query parameters available in a send operation.
 */

export enum SendOperationParameter {
    Address = 'address',
    Amount = 'amount',
    Unit = 'unit',
    Metadata = 'metadata',
    Tag = 'tag',
}
